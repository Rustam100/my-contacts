export interface Contacts {
    contactList: ContactParams[];
}

export interface ContactParams {
    name: string;
    surname: string;
    email: string;
    phoneNumber: string;
    sex: string;
}

